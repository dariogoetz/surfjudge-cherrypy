[directories]
basedir   = path(default='remote/cherrypy/')
config    = path(default='cfg')
logs      = path(default='logs')
templates = path(default='view')
template_cache = path(default = None)

[config_files]
server = path(default='server.cfg')
SurfJudgeWebInterface = path(default='SurfJudgeWebInterface.cfg')
AuthenticationController = path(default='AuthenticationController.cfg')
[logger]
max_bytes      = int(default=10485760)
backup_count   = int(default=5)
access_logfile = path(default='access.log')
error_logfile  = path(default='error.log')